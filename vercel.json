{
  "version": 2,
  "builds": [
    {
      "src": "quickai-client/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/quickai-client/dist/$1"
    }
  ]
}
